# Credentials — never commit. This repo is public.
.env
.env.*
!.env.example
*.pem
*.key

# gh-aw local run artifacts
/tmp/gh-aw/
.gh-aw/

# Editor / OS — keep shareable VS Code config, ignore the rest
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/mcp.json
.DS_Store
*.swp

# local run state (pids, logs)
.run/
